# Screenshots

Place the screenshot(s) here:

- `depth-mode-3d.png` - The 3D visualization showing depth mode with the staircase effect

The main screenshot should show:
- 3D treemap with depth visualization active
- Color gradient from blue (shallow) to red (deep)
- Statistics panel on the right
- File Type Breakdown legend
- Buttons showing 3D + Depth modes selected

Image dimensions: ~1600x800 or similar aspect ratio
